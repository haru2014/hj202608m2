제시해주신 미션을 완성하기 위해 초보자 눈높이에 맞춰 **[1단계] API 서비스 회원가입 및 키 발급**부터 **[2단계] 파이썬 개발환경 구축**, **[3단계] 코드 작성 및 파일 생성**, **[4단계] 실행 및 테스트**까지 단계별 실행 계획을 정리해 드립니다.

여기서는 가장 보편적이고 설정이 직관적인 조합인 **Google Gemini API(LLM)** + Kakao Local API(지도/장소 검색)를 기준으로 설명합니다.

---

## 1단계: API 회원가입 및 API 키 발급 받기

프로그램이 외부 서비스와 통신하려면 회원가입 후 **비밀번호 역할을 하는 API Key**를 발급받아야 합니다.

### 1-1. Google Gemini API 키 발급 (LLM용)

1. **Google AI Studio 접속**: 브라우저에서 Google AI Studio 페이지로 이동합니다.
2. **구글 로그인**: 기존 구글 계정으로 로그인합니다.
3. **API 키 생성**:
* 화면 좌측 상단 또는 중앙의 **[Get API key]** 버튼을 클릭합니다.
* [Create API key]를 누른 뒤, 새 프로젝트를 생성하거나 기존 프로젝트를 선택합니다.


4. **키 복사 및 보관**:
* `AIzaSy...` 형태로 생성된 문자열을 복사하여 메모장에 임시로 저장해 둡니다.



### 1-2. Kakao Developers API 키 발급 (장소 검색용)

1. **Kakao Developers 접속**: 카카오 개발자 센터 페이지에 접속 후 카카오 계정으로 로그인합니다.
2. **애플리케이션 추가**:
* 상단 메뉴의 **[내 애플리케이션]** -> **[애플리케이션 추가하기]** 클릭
* **앱 이름**(예: `TravelPlanner`)과 **사업자명**(예: `개인`)을 입력하고 저장합니다.


3. **REST API 키 확인**:
* 생성된 앱을 클릭하여 **[앱 키]** 항목을 확인합니다.
* **`REST API 키`** 값(32자리 문자열)을 복사하여 메모장에 임시 저장합니다.



---

## 2단계: 개발 환경 구성 및 보안 설정

API 키를 코드 안에 직접 적으면 외부 유출 위험이 있으므로, 환경 변수 파일(`.env`)을 통해 관리합니다.

### 2-1. 작업 폴더 생성 및 VS Code 열기

1. 컴퓨터의 원하는 위치에 `travel_project` 폴더를 만듭니다.
2. VS Code(또는 사용 중인 에디터)에서 이 폴더를 열고 터미널(Terminal)을 엽니다.

### 2-2. 파이썬 필수 라이브러리 설치

터미널에 아래 명령어를 입력하여 필요한 라이브러리를 설치합니다.

```bash
pip install google-genai requests python-dotenv

```

* `google-genai`: Google Gemini API 공식 SDK
* `requests`: Kakao HTTP API 호출용
* `python-dotenv`: `.env` 파일의 환경변수를 읽어오는 라이브러리

### 2-3. `.env` 파일 생성 (보안 관리)

`travel_project` 폴더 내에 `.env`라는 이름의 파일을 만들고 아래와 같이 발급받은 키를 입력합니다.

```env
GEMINI_API_KEY=1단계에서_발급받은_Gemini_API_키
KAKAO_REST_API_KEY=1단계에서_발급받은_Kakao_REST_API_키

```

---

## 3단계: 소스 코드 및 제출 파일 작성

폴더 내에 아래 파일들을 순서대로 생성합니다.

### 3-1. 메인 프로그램 작성 (`travel_planner.py`)

아래 코드는 예외 처리(401/403 인증 오류, JSON 파싱 실패 재시도 1회 등)와 요구사항을 충족하도록 구성된 전체 소스코드입니다.

```python
import argparse
import json
import os
import sys
import re
import requests
from dotenv import load_dotenv
from google import genai
from google.genai import types

# 1. 환경 변수 로드
load_dotenv()

GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")
KAKAO_REST_API_KEY = os.getenv("KAKAO_REST_API_KEY")

# API 키 설정 검증
if not GEMINI_API_KEY or not KAKAO_REST_API_KEY:
    print("[ERROR] API 키가 설정되지 않았습니다.")
    print(".env 파일에 GEMINI_API_KEY와 KAKAO_REST_API_KEY가 바르게 입력되었는지 확인하세요.")
    sys.exit(1)

# Gemini 클라이언트 초기화
gemini_client = genai.Client(api_key=GEMINI_API_KEY)

# 2. CLI 인자 파싱 및 입력값 검증
def parse_arguments():
    parser = argparse.ArgumentParser(description="국내 여행지 및 맛집 추천 프로그램")
    parser.add_argument("--date", required=True, help="여행 날짜 (YYYY-MM-DD)")
    args = parser.parse_args()

    # YYYY-MM-DD 정규식 검증
    date_pattern = r"^\d{4}-\d{2}-\d{2}$"
    if not re.match(date_pattern, args.date):
        print("[ERROR] 올바르지 않은 날짜 형식입니다. 'YYYY-MM-DD' 형식으로 입력해주세요.")
        parser.print_help()
        sys.exit(1)
    return args.date

# 3. LLM Step 1: 1차 추천 (JSON 파싱)
def get_llm_recommendation(date_str, errors_list, is_retry=False):
    prompt = f"""
    당신은 여행 전문가입니다. 여행 날짜 '{date_str}'에 가기 좋은 한국의 도시 1곳을 추천해주세요.
    응답은 반드시 아래 JSON 스키마를 엄격히 준수하여 오직 JSON 형식의 텍스트만 출력하세요. 다른 설명은 포함하지 마세요.

    JSON 스키마:
    {{
      "recommended_city": "도시명 (예: 제주, 강릉)",
      "weather": "해당 시기 일반적 날씨 요약",
      "events": ["행사 또는 축제 후보 1~3개"],
      "reason": "추천 근거 2~4문장"
    }}
    """
    if is_retry:
        prompt += "\n주의: 이전 응답의 JSON 파싱에 실패했습니다. 규칙에 맞춰 정확한 JSON만 반환하세요."

    try:
        response = gemini_client.models.generate_content(
            model="gemini-2.5-flash",
            contents=prompt,
            config=types.GenerateContentConfig(
                response_mime_type="application/json"
            )
        )
        data = json.loads(response.text)
        return data
    except Exception as e:
        if not is_retry:
            print("  [경고] LLM JSON 파싱 실패. 1회 재시도합니다...")
            return get_llm_recommendation(date_str, errors_list, is_retry=True)
        else:
            errors_list.append({
                "step": "llm_recommendation",
                "type": "PARSE_ERROR",
                "message": f"JSON 파싱 최종 실패: {str(e)}"
            })
            return {
                "recommended_city": "제주",
                "weather": "날씨 정보 파싱 실패",
                "events": [],
                "reason": "기본 추천 도시로 대체되었습니다."
            }

# 4. Kakao Local API: 맛집 검색
def search_places(city, errors_list):
    headers = {
        "Authorization": f"KakaoAK {KAKAO_REST_API_KEY}"
    }
    url = "https://dapi.kakao.com/v2/local/search/keyword.json"
    params = {
        "query": f"{city} 맛집",
        "size": 5
    }

    places = []
    try:
        res = requests.get(url, headers=headers, params=params, timeout=5)
        if res.status_code in [401, 403]:
            print(f"  - 오류: 지도 API 인증 실패({res.status_code}). 키/권한 설정을 확인하세요.")
            errors_list.append({
                "step": "place_search",
                "type": "AUTH_ERROR",
                "message": f"HTTP {res.status_code}"
            })
            return []
        
        res.raise_for_status()
        result = res.json()
        documents = result.get("documents", [])

        if not documents:
            print("  - 검색 결과 0건 (데이터 없음으로 진행)")
            errors_list.append({
                "step": "place_search",
                "type": "EMPTY_RESULT",
                "message": f"0 results for query={city} 맛집"
            })
            return []

        for doc in documents:
            places.append({
                "name": doc.get("place_name", ""),
                "address": doc.get("road_address_name") or doc.get("address_name", ""),
                "category": doc.get("category_name", ""),
                "url": doc.get("place_url", ""),
                "x": doc.get("x", ""),
                "y": doc.get("y", "")
            })
        print(f"  - 맛집 {len(places)}곳 검색 완료")
        return places

    except Exception as e:
        print(f"  - 지도 API 호출 중 오류 발생: {e}")
        errors_list.append({
            "step": "place_search",
            "type": "NETWORK_OR_API_ERROR",
            "message": str(e)
        })
        return []

# 5. LLM Step 2: 최종 여행 리포트 생성 (Markdown)
def generate_final_report(date_str, rec_data, places, errors_list):
    places_str = json.dumps(places, ensure_ascii=False, indent=2) if places else "데이터 없음"
    errors_str = json.dumps(errors_list, ensure_ascii=False, indent=2) if errors_list else "없음"

    prompt = f"""
    당신은 여행 리포트 작성가입니다. 아래 제공된 정보로 여행 리포트를 Markdown 형식으로 작성해주세요.

    [기본 정보]
    - 여행 날짜: {date_str}
    - 추천 지역: {rec_data.get('recommended_city')}
    - 추천 이유: {rec_data.get('reason')}
    - 날씨 요약: {rec_data.get('weather')}
    - 행사/축제: {', '.join(rec_data.get('events', []))}
    - 맛집 정보: {places_str}
    - 발생한 오류: {errors_str}

    [작성 규칙]
    - 마크다운 헤더(#, ##)를 활용하여 정돈된 문서로 만드세요.
    - 맛집 정보가 '데이터 없음'이거나 빈 배열이면 맛집 섹션에 "데이터 없음 (장소 검색 결과 0건)" 표기하세요.
    - 오전/오후/저녁으로 나눈 1일 일정 제안 코너를 포함하세요.
    - 오류 내역(errors) 섹션을 마지막에 포함하세요.
    """

    try:
        response = gemini_client.models.generate_content(
            model="gemini-2.5-flash",
            contents=prompt
        )
        return response.text
    except Exception as e:
        errors_list.append({
            "step": "report_generation",
            "type": "LLM_ERROR",
            "message": str(e)
        })
        return f"# {date_str} 여행 리포트 생성 실패\n\n리포트 생성 중 오류가 발생했습니다: {e}"

# 6. 메인 실행 제어
def main():
    date_str = parse_arguments()
    errors_list = []

    os.makedirs("results", exist_ok=True)

    print(f"[1/3] 1차 추천 생성 중(LLM)...")
    rec_data = get_llm_recommendation(date_str, errors_list)
    city = rec_data.get("recommended_city", "제주")
    print(f"  - recommended_city: \"{city}\"")

    print(f"[2/3] 맛집 검색 중(지도/장소 API)...")
    places = search_places(city, errors_list)

    print(f"[3/3] 최종 리포트 생성 중(LLM)...")
    report_md = generate_final_report(date_str, rec_data, places, errors_list)
    print("  - 리포트 생성 완료")

    # 결과 데이터 저장
    raw_data = {
        "date": date_str,
        "recommendation": rec_data,
        "places": places,
        "errors": errors_list
    }
    
    json_path = os.path.join("results", f"{date_str}_raw.json")
    md_path = os.path.join("results", f"{date_str}_travel_plan.md")

    with open(json_path, "w", encoding="utf-8") as f:
        json.dump(raw_data, f, ensure_ascii=False, indent=2)

    with open(md_path, "w", encoding="utf-8") as f:
        f.write(report_md)

    print(f"\n완료! {md_path} 및 {json_path} 를 확인하세요.")

if __name__ == "__main__":
    main()

```

### 3-2. `README.md` 작성

프로젝트 설명을 담은 문서 파일입니다.

```markdown
# 국내 여행지 추천 프로그램

사용자가 지정한 날짜에 맞는 최적의 국내 여행지를 AI가 추천하고, 카카오 장소 검색 API를 연동하여 해당 지역의 맛집 정보 및 1일 여행 일정을 담은 리포트를 자동 생성합니다.

## 실행 방법

1. 필수 라이브러리 설치
   ```bash
   pip install google-genai requests python-dotenv

```

2. 프로그램 실행 (날짜 인자 필수)
```bash
python travel_planner.py --date "2026-05-15"

```



## API 키 설정 방법 및 보안 주의사항

1. 프로젝트 루트 디렉터리에 `.env` 파일을 생성합니다.
2. 아래와 같은 형식으로 발급받은 API 키를 작성합니다.
```env
GEMINI_API_KEY=your_gemini_api_key_here
KAKAO_REST_API_KEY=your_kakao_rest_api_key_here

```


3. **주의사항**: API 키가 Git 커밋이나 공유 파일에 포함되지 않도록 `.env` 파일은 절대 외부로 유출하지 마세요.

## 결과물 확인

실행이 완료되면 `results/` 폴더 내에 아래 파일이 생성됩니다.

* `YYYY-MM-DD_raw.json`: 1차 추천 데이터 및 맛집 검색 결과 원본
* `YYYY-MM-DD_travel_plan.md`: 최종 여행 리포트 Markdown 문서

```

---

## 4단계: 실행 및 검증 (Test Case)

터미널에서 명령어를 입력하여 정상 작동 여부와 예외 처리를 검증합니다.

### 4-1. 정상 실행 테스트
```bash
python travel_planner.py --date "2026-05-15"

```

* **기대 결과**:
* `[1/3]`, `[2/3]`, `[3/3]` 단계별 로그 출력
* `results/2026-05-15_raw.json` 및 `results/2026-05-15_travel_plan.md` 파일 생성 확인



### 4-2. 입력값 예외 처리 테스트

```bash
python travel_planner.py --date "20260515"

```

* **기대 결과**: `[ERROR] 올바르지 않은 날짜 형식입니다.` 메시지가 출력되고 프로세스가 종료됨.

### 4-3. API 키 미설정 테스트

`.env` 파일의 키 이름을 임시로 변경 후 실행:

* **기대 결과**: `[ERROR] API 키가 설정되지 않았습니다.` 메시지와 안내 문구 출력 후 종료.