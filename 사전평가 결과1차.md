AI 사전평가 결과
평가 시각: 2026-08-14 18:29:04

76%
(13 / 17 항목 통과)
종합 평가
입력-LLM-검색-리포트의 핵심 CLI 흐름과 안정성 조치(날짜검증, LLM JSON 강제·재시도, 정규화, 오류 누적 및 결과 저장)가 잘 구현되어 있습니다. 다만 지도 API 교체 추상화, 엄격한 스키마 필수키 검증, 재실행 캐시 등 설계·검증 보완이 필요합니다.

항목별 평가 (17개)
PASS
평가 항목 #1
근거
travel_planner.py > 'parser.add_argument("--date", required=True, help="여행 날짜 (YYYY-MM-DD)")'
잘한 점
CLI 옵션과 YYYY-MM-DD 정규식 검증을 구현함.
부족한 점
존재하지 않는 날짜(예: 2026-02-30) 같은 추가 유효성 검사는 없음.
보완
존재하지 않는 날짜 사례에 대한 추가 검증 로직을 고려하세요.
PASS
평가 항목 #2
근거
travel_planner.py > '"recommended_city": "도시명 (예: 제주, 강릉)"'
잘한 점
LLM에게 엄격한 JSON 스키마를 제시함.
부족한 점
스키마 필드별 엄격한 존재·타입 검사 설명은 부족함.
보완
스키마 필수키 누락 시 동작(로그/대체값)을 README에 명시하세요.
PASS
평가 항목 #3
근거
travel_planner.py > 'params = {"query": f"{city} 맛집", "size": 5}'
잘한 점
추천 도시는 실제로 장소 검색 쿼리로 사용됨.
부족한 점
지도 API 호출이 Kakao로 고정되어 있어 교체 대비가 없음.
보완
검색 실패·대체 흐름과 교체 방안(문서)을 보강하세요.
PASS
평가 항목 #4
근거
travel_planner.py > 'json_path = os.path.join(RESULTS_DIR, f"{date_str}_raw.json")'
잘한 점
원본 JSON과 Markdown을 results/에 파일로 저장함.
부족한 점
파일명 충돌·동시 실행 정책(덮어쓰기 등)이 명시되어 있지 않음.
보완
결과 저장의 overwrite 정책 또는 버전(타임스탬프) 옵션을 문서화하세요.
PASS
평가 항목 #5
근거
travel_planner.py > 'GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")'
잘한 점
환경변수로 키를 로드하여 하드코딩을 피함.
부족한 점
결과물 저장 시 API 키 노출을 자동 검사하는 절차는 없음.
보완
결과 파일 저장 전 민감정보(키) 패턴 검사를 추가하세요.
PASS
평가 항목 #6
근거
README.md > '[사용자 날짜 입력] ──> [Gemini API (JSON 응답)] ──> [Robust JSON 파싱 & 도시명 정규화] '
잘한 점
입력-검색-리포트의 처리 흐름을 문서로 명확히 제시함.
부족한 점
파일/함수별 책임 매핑(함수↔모듈 설명)이 부족함.
보완
각 함수·모듈의 책임을 README에 간단 매핑으로 추가하세요.
FAIL
평가 항목 #7
근거
travel_planner.py > '"events": data.get("events", []) if isinstance(data.get("events", []), list) else []'
잘한 점
부분적 타입 검사(events)와 기본값 대체를 구현함.
부족한 점
필수 키 존재 검증과 엄격한 타입 검사(예: recommended_city 필수)가 구현되지 않음.
보완
필수키 미존재 시 errors에 기록하고 명확한 실패 처리 정책을 추가하세요.
FAIL
평가 항목 #8
근거
travel_planner.py > 'url = "https://dapi.kakao.com/v2/local/search/keyword.json"'
잘한 점
Kakao Local API 연동이 명확히 구현되어 있음.
부족한 점
지도 API 교체를 위한 추상화 레이어나 플러그인형 구조가 없음.
보완
지도 제공자 추상 인터페이스를 도입하여 설정만으로 교체 가능하도록 설계하세요.
PASS
평가 항목 #9
근거
travel_planner.py > 'raw_data = {"date": date_str, "recommendation": rec_data, "places": places, "errors": errors_list}'
잘한 점
errors 리스트에 오류를 누적하고 결과 JSON 및 리포트에 반영함.
부족한 점
누적 에러의 장기 보존(히스토리 파일) 정책은 문서화되어 있지 않음.
보완
errors를 파일로 날짜별 누적 저장하는 옵션을 고려하세요.
FAIL
평가 항목 #10
근거
travel_planner.py > 'res = requests.get(url, headers=headers, params=params, timeout=5)'
잘한 점
장소 조회에 적절히 GET을 사용함.
부족한 점
GET/POST 사용 기준(예: LLM은 POST 등)에 대한 설계적 이유 설명이 문서에 없음.
보완
README에 각 API 호출의 HTTP 메서드 선택 이유를 명확히 적어두세요.
PASS
평가 항목 #11
근거
travel_planner.py > 'response_mime_type="application/json"'
잘한 점
코드에서 LLM 출력의 MIME 타입으로 JSON 강제를 적용함.
부족한 점
구조화 장점(후처리·검증)에 대한 구체적 사례 설명이 더 필요함.
보완
JSON 강제로 인한 후처리 파이프라인 예시를 README에 추가하세요.
PASS
평가 항목 #12
근거
travel_planner.py > 'if res.status_code in [401, 403]: print(f" - 오류: 지도 API 인증 실패({res.status_code}). 키/권한 설정을 확인하세요.")'
잘한 점
401/403 발생 시 사용자 메시지 출력과 errors 기록을 수행함.
부족한 점
401/403 원인(쿼터·권한·헤더)별 진단 체크리스트는 문서화되어 있지 않음.
보완
README에 401/403 원인별 점검 항목(헤더/키/쿼터)을 추가하세요.
PASS
평가 항목 #13
근거
README.md > 'GEMINI_API_KEY=your_gemini_api_key_here'
잘한 점
README에서 .env 사용법과 보안 이점을 명시함.
부족한 점
운영 환경(시크릿 매니저) 통합 안내는 없음.
보완
배포 시 시크릿 매니저 연동 권장 가이드를 추가하세요.
PASS
평가 항목 #14
근거
travel_planner.py > 'print(" [경고] LLM JSON 파싱 실패. 1회 재시도합니다...")'
잘한 점
파싱 실패 시 1회 재요청과 프롬프트 수정 재시도 로직을 구현함.
부족한 점
재시도 정책에 대한 백오프·횟수 한도 등 상세 정책은 없음.
보완
재시도 실패 시 대체 흐름 및 백오프/한도 정책을 문서화하세요.
PASS
평가 항목 #15
근거
travel_planner.py > 'print(" - 검색 결과 0건 (데이터 없음으로 진행)")'
잘한 점
맛집 0건일 때 중단하지 않고 '데이터 없음'으로 표기하여 리포트 진행함.
부족한 점
리포트 내에서 '데이터 없음'의 시각적 강조나 대체 제안은 미흡함.
보완
리포트 템플릿에서 '데이터 없음' 표기를 가독성 있게 개선하세요.
FAIL
평가 항목 #16
근거
travel_planner.py > 'with open(json_path, "w", encoding="utf-8") as file_obj:'
잘한 점
결과를 results/에 저장해 재사용 가능성을 열어둠.
부족한 점
동일 날짜 재실행 시 기존 results 재사용(캐시) 로직이 없음.
보완
재실행 시 기존 결과 확인 후 API 호출을 건너뛰는 캐시 로직을 추가하세요.
PASS
평가 항목 #17
근거
travel_planner.py > "city = re.sub(r'특별자치도|특별시|광역시|도$', '', city).strip()"
잘한 점
도시명 정규화 함수로 접미사·특수문자 제거를 구현함.
부족한 점
오탈자 보정·키워드 보정이나 재질문 복구 로직은 부분 미구현임.
보완
간단한 키워드 사전 또는 재질문 루틴으로 정규화 범위를 확장하세요.