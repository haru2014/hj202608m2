# 1단계 개발 계획: API 회원가입 및 키 발급

## 목표

외부 서비스와 연결되는 프로그램을 위해 필수적인 API 계정과 키를 준비한다.

## 주요 작업

1. Google AI Studio 접속
2. Gemini API 키 발급
3. Kakao Developers 접속
4. Kakao REST API 키 발급
5. 키 값 임시 저장 및 보안 관리

## 보안 원칙

- API 키를 코드에 직접 포함하지 않는다.
- `.env` 파일에 별도 저장한다.
- 키를 공유 문서나 Git 저장소에 넣지 않는다.

## 산출물

- Gemini API 키
- Kakao REST API 키
- `.env` 파일 구성용 값

## 검증 기준

- API 키가 정상 발급되었는지 확인
- 키 문자열이 복사되었는지 확인
- `.env`에 저장될 변수명 확인

## 기대 결과

다음 단계에서 프로그램이 외부 API와 정상 통신할 수 있는 기반을 확보한다.
