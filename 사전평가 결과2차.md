AI 사전평가 결과
평가 시각: 2026-08-14 18:49:11

71%
(12 / 17 항목 통과)
종합 평가
핵심 CLI 흐름(날짜 검증 → LLM 추천 JSON → 도시 정규화 → Kakao 장소 검색 → Markdown 리포트 저장)이 함수 단위로 구현되어 결과 저장·오류 누적·재시도 정책 등이 잘 갖춰져 있습니다. 다만 스키마의 엄격한 필수키 검증, 지도 API 교체 추상화, 재실행 캐시, 추천도시 정규화(오탈자·세부지역 추출·재질문) 등 설계·검증 보완이 필요합니다.

항목별 평가 (17개)
PASS
평가 항목 #1
근거
travel_planner.py > 'parser.add_argument("--date", required=True, help="여행 날짜 (YYYY-MM-DD)")'
잘한 점
CLI 옵션과 YYYY-MM-DD 형식 검증을 구현함.
부족한 점
존재하지 않는 날짜(예: 2026-02-30) 같은 추가 유효성 검사는 없음.
보완
입력 형식 검사 후 datetime.strptime로 실제 날짜 유효성 검증을 추가하세요.
PASS
평가 항목 #2
근거
travel_planner.py > '"recommended_city": "도시명 (예: 제주, 강릉)"'
잘한 점
LLM에 엄격한 1차 JSON 스키마를 프롬프트로 제시함.
부족한 점
스키마 필드별 엄격한 존재·타입 검사 구현은 미흡함.
보완
응답 후 필수키 존재 여부와 타입을 명시적으로 체크하는 검증 단계를 문서화하세요.
PASS
평가 항목 #3
근거
travel_planner.py > 'params = {"query": f"{city} 맛집", "size": 5}'
잘한 점
추천 도시를 실제 장소 검색 쿼리로 사용하고 Kakao API 호출로 연결함.
부족한 점
지도 호출 실패 시 빈 리스트로 진행하는 등 중단 없이 흐름을 유지하지만 재시도/대체 키워드 전략은 미흡.
보완
검색 실패 시 키워드 변형이나 지역 확대 같은 간단한 재시도 전략을 추가 고려하세요.
PASS
평가 항목 #4
근거
travel_planner.py > 'json_path = os.path.join(RESULTS_DIR, f"{date_str}_raw.json")'
잘한 점
results/에 원본 JSON과 Markdown 결과를 파일로 저장함.
부족한 점
동일 파일명 충돌·버전 정책(덮어쓰기·타임스탬프 등)이 명시되어 있지 않음.
보완
저장 정책(overwrite/버전화)을 README에 명확히 기술하세요.
PASS
평가 항목 #5
근거
travel_planner.py > 'GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")'
잘한 점
API 키를 환경변수로 로드하여 코드에 하드코딩하지 않음.
부족한 점
결과물(파일)에 키 노출 자동 검사 절차는 없음.
보완
저장 전에 민감값 패턴 검사(예: API 키 정규식)로 노출 여부를 검증하세요.
PASS
평가 항목 #6
근거
README.md > '[사용자 날짜 입력] ──> [Gemini API (JSON 응답)] ──> [Robust JSON 파싱 & 도시명 정규화] '
잘한 점
입력-검색-리포트 흐름과 함수 분리가 문서와 코드에서 일치함.
부족한 점
함수/모듈별 책임(파일↔함수 매핑) 설명은 간단히 보강 필요.
보완
README에 각 주요 함수의 책임(입력/검색/리포트)을 표기하세요.
FAIL
평가 항목 #7
근거
travel_planner.py > '"events": data.get("events", []) if isinstance(data.get("events", []), list) else []'
잘한 점
events에 대해 부분적 타입 검사와 기본값 대체를 구현함.
부족한 점
필수 키(recommended_city 등) 존재 검증과 엄격한 타입 검사가 구현되지 않았습니다.
보완
필수키 누락 시 errors에 기록하고, 복구/대체 정책을 명시적으로 추가하세요.
FAIL
평가 항목 #8
근거
travel_planner.py > 'url = "https://dapi.kakao.com/v2/local/search/keyword.json"'
잘한 점
Kakao Local API 연동이 명확히 구현되어 있음.
부족한 점
지도 API 교체를 위한 추상화 레이어·플러그인형 구조가 없음(설정만으로 교체 불가).
보완
지도 검색 호출을 추상화한 인터페이스(예: provider.search_places)를 도입하여 교체를 용이하게 만드세요.
PASS
평가 항목 #9
근거
travel_planner.py > 'raw_data = {"date": date_str, "recommendation": rec_data, "places": places, "errors": errors_list}'
잘한 점
에러를 errors 리스트에 누적하고 결과 JSON/리포트에 반영함.
부족한 점
errors 장기 보존(히스토리) 정책은 문서화되어 있지 않음.
보완
errors를 날짜별 로그 또는 누적 파일로 저장하는 옵션을 고려하세요.
FAIL
평가 항목 #10
근거
travel_planner.py > 'res = requests.get(url, headers=headers, params=params, timeout=5)'
잘한 점
장소 조회에 적절히 GET을 사용하고 있음.
부족한 점
GET/POST 선택 이유(예: LLM은 POST로 생성 요청)와 설계적 근거를 문서로 설명하지 않음.
보완
README에 각 API 호출에서 HTTP 메서드 선택 이유를 명확히 기술하세요.
PASS
평가 항목 #11
근거
travel_planner.py > 'response_mime_type="application/json"'
잘한 점
LLM 응답을 JSON으로 강제하여 후처리와 안정적 파싱이 용이함.
부족한 점
구조화 장점과 후처리 사례(구체 예시)를 README에 더 자세히 설명하면 좋음.
보완
JSON 강제의 후처리 이점(예: downstream 파이프라인 예시)을 문서에 추가하세요.
PASS
평가 항목 #12
근거
travel_planner.py > 'if res.status_code in [401, 403]: print(f" - 오류: 지도 API 인증 실패({res.status_code}). 키/권한 설정을 확인하세요.")'
잘한 점
401/403 발생 시 사용자 메시지와 errors 기록으로 디버깅 단서를 제공함.
부족한 점
401/403 원인별(쿼터/헤더/권한) 점검 체크리스트 문서화는 부족함.
보완
README에 401/403 원인별 점검 항목(헤더/키/도메인/쿼터)을 포함하세요.
PASS
평가 항목 #13
근거
README.md > 'GEMINI_API_KEY=your_gemini_api_key_here'
잘한 점
README에 .env 사용법과 보안 이점을 명시함.
부족한 점
운영 환경(시크릿 매니저) 통합 가이드는 없음.
보완
배포 시 시크릿 매니저 연동 권장 방안을 문서에 추가하세요.
PASS
평가 항목 #14
근거
travel_planner.py > 'print(" [경고] LLM JSON 파싱 실패. 1회 재시도합니다...")'
잘한 점
LLM JSON 파싱 실패 시 1회 재시도 및 프롬프트 수정 로직을 구현함.
부족한 점
재시도 정책의 백오프·횟수 한도 등의 상세 정책은 문서화되어 있지 않음.
보완
재시도 한도와 백오프 정책을 README에 명시하세요.
PASS
평가 항목 #15
근거
travel_planner.py > 'print(" - 검색 결과 0건 (데이터 없음으로 진행)")'
잘한 점
맛집 0건일 때 중단하지 않고 '데이터 없음'으로 표기하여 리포트 진행함.
부족한 점
리포트 내 '데이터 없음' 표기의 가독성 향상(대체 제안 등)은 개선 여지 있음.
보완
리포트 템플릿에서 '데이터 없음'을 더 눈에 띄게 표기하거나 대체 제안을 추가하세요.
FAIL
평가 항목 #16
부족한 점
동일 날짜 재실행 시 기존 results 재사용(캐시) 로직이 미구현되어 비용 절감 기능이 없음.
보완
재실행 시 results 폴더에 기존 raw.json이 있으면 API 호출을 건너뛰는 캐시 체크를 추가하세요.
FAIL
평가 항목 #17
근거
travel_planner.py > 'city = re.sub(r"특별자치도|특별시|광역시|도$", "", city).strip()'
잘한 점
행정구역 접미사 제거 등 기본 도시명 정규화를 구현함.
부족한 점
키워드 보정·세부지역 추출·재질문 같은 고급 정규화·복구 로직은 미구현입니다.
보완
간단한 키워드 사전과 재질문 루틴으로 오탈자 보정 및 세부지역 추출을 추가하세요.