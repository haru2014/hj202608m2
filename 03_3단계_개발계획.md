# 3단계 개발 계획: 코드 작성, 실행, 테스트

## 목표

구현된 설계안을 실제 Python 프로그램으로 작성하고, 제대로 동작하는지 실행 및 검증을 수행한다.

## 핵심 작업

1. 프로그램 코드 작성
   - CLI 인자 처리
   - Gemini API 호출
   - Kakao Local API 호출
   - 결과 저장 및 리포트 생성

2. 파일 구조 작성
   - travel_planner.py
   - README.md
   - .env
   - .env.example
   - requirements.txt
   - .gitignore
   - results 폴더

3. 실행 검증
   - 정상 입력으로 실행
   - 잘못된 날짜 형식 검증
   - API 키 미설정 테스트
   - 결과 파일 생성 확인

## 구현 원칙

- API 키는 `.env`에서 관리한다.
- 예외 상황은 로그로 남기고 프로그램을 안전하게 종료하거나 다음 단계로 진행한다.
- 결과 데이터를 JSON으로 저장한다.
- 최종 결과를 Markdown 형태로 출력한다.

## 검증 기준

- 프로그램이 정상 실행된다.
- `results/` 폴더에 파일이 생성된다.
- 잘못된 입력값에 대해 에러 메시지가 출력된다.
- API 키 누락 시 안내 메시지가 표시된다.

## 기대 결과

최종적으로, 미션 요구사항을 만족하는 CLI 기반 여행 추천 프로그램이 완성된다.
