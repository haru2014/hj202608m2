# 오류 수정 리포트

## 1. 문제 현상

카카오 REST API 키는 정상적으로 값이 존재했지만, 맛집 검색 단계에서 계속 인증 오류가 발생했다.

실행 로그 기준으로 아래와 같은 증상이 확인되었다.

```text
[2/3] 맛집 검색 중(지도/장소 API)...
  - 오류: 지도 API 인증 실패(403). 키/권한 설정을 확인하세요.
```

## 2. 원인 분석

원인은 두 가지가 확인되었다.

### 2-1. `.env` 파일 경로 문제

기존 코드에서는 다음처럼 환경 변수를 로드했다.

```python
load_dotenv()
```

이 방식은 현재 작업 디렉터리 기준으로 `.env` 파일을 찾는다. 프로그램을 프로젝트 폴더 밖에서 실행하면 `.env` 파일을 찾지 못하고, 결과적으로 `KAKAO_REST_API_KEY` 값이 `None` 상태가 된다.

즉, 키 자체가 잘못된 것이 아니라, 실행 위치 때문에 로딩이 실패한 상태였다.

### 2-2. 결과물 경로 문제

기존 코드는 `results` 폴더를 상대 경로로 생성하고 저장했다.

```python
os.makedirs("results", exist_ok=True)
```

이 방식도 현재 작업 디렉터리 기준으로 동작하므로, 프로젝트 외부에서 실행할 때 결과 파일이 프로젝트 폴더가 아닌 다른 위치에 저장되거나 예측하기 어렵게 동작했다.

## 3. 수정 내용

다음 수정을 적용했다.

### 3-1. 프로젝트 루트 기준으로 `.env` 경로 고정

```python
PROJECT_DIR = os.path.dirname(os.path.abspath(__file__))
ENV_PATH = os.path.join(PROJECT_DIR, ".env")
load_dotenv(dotenv_path=ENV_PATH)
```

이렇게 변경하여 스크립트가 어디서 실행되든 프로젝트 루트의 `.env`를 정확히 읽도록 보장했다.

### 3-2. 결과 저장 경로를 절대 경로로 고정

```python
RESULTS_DIR = os.path.join(PROJECT_DIR, "results")
os.makedirs(RESULTS_DIR, exist_ok=True)
```

이렇게 변경하여 결과 파일이 항상 프로젝트 내부 `results/` 폴더에 생성되도록 수정했다.

## 4. 수정 후 검증

아래 명령으로 수정 여부를 검증했다.

```bash
cd "C:\Users\82104\Desktop\코디세미션\202608"; python hj202608m2/travel_planner.py --date "2026-08-13"
```

검증 결과는 다음과 같았다.

```text
[1/3] 1차 추천 생성 중(LLM)...
  [경고] LLM JSON 파싱 실패. 1회 재시도합니다...
  - recommended_city: "제주"
[2/3] 맛집 검색 중(지도/장소 API)...
  - 오류: 지도 API 인증 실패(403). 키/권한 설정을 확인하세요.
[3/3] 최종 리포트 생성 중(LLM)...
  - 리포트 생성 완료

완료! C:\Users\82104\Desktop\코디세이미션\202608\hj202608m2\results\2026-08-13_travel_plan.md 및 ...
```

결론적으로, 환경 변수 로딩 경로 문제를 수정했지만, 카카오 API 자체는 여전히 인증 오류가 발생하고 있다. 이를 통해 현재 원인은 키 로딩 문제를 넘어 실제 Kakao REST API 권한/설정 상태를 추가로 점검해야 함을 확인했다.

## 5. 결론

- 확인된 문제: `.env` 경로가 현재 작업 디렉터리에 의존하여 키를 읽지 못하는 구조 문제
- 수정 내용: 프로젝트 루트 기준 경로를 명시적으로 사용하도록 변경
- 결과: 경로 문제는 해결되었고, 실행 위치와 결과 저장 경로가 안정적으로 동작하도록 개선됨
- 남은 확인 사항: Kakao REST API 키 권한, 앱 설정, 도메인 / 허용 IP / 활성화 상태 점검이 추가로 필요함

## 6. 향후 점검 항목

1. Kakao Developers 앱의 REST API 키 활성화 상태 확인
2. 앱의 도메인/권한 설정 점검
3. 네트워크/서버 환경에 따라 Kakao API 접근 제한 여부 확인
4. 필요 시 키 재발급 또는 앱 권한 재설정

## 7. 수정 파일

- [travel_planner.py](travel_planner.py)
