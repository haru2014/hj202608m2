# 2단계 개발 계획: API 연동형 여행 추천 프로그램

## 1. 목표

이번 단계에서는 국내 여행 추천 프로그램의 실제 동작 흐름을 설계하고, 외부 API와 Python 코드를 연결하는 구조를 구현한다.

핵심 목표는 다음과 같다.

- Gemini API를 활용해 여행 추천 도시를 생성한다.
- Kakao Local API를 활용해 추천 도시의 맛집을 검색한다.
- 결과를 JSON과 Markdown으로 저장한다.
- API 인증 실패, JSON 파싱 실패, 검색 결과 0건 상황을 안전하게 처리한다.
- README와 실행 문서를 통해 프로젝트를 재현할 수 있게 한다.

## 2. 구현 범위

### 2-1. 환경 구성

- Python 3.10 이상 환경 확인
- 필요한 라이브러리 설치
  - google-genai
  - requests
  - python-dotenv
- .env 파일에 키 저장
  - GEMINI_API_KEY
  - KAKAO_REST_API_KEY

### 2-2. 코드 구조 설계

1. CLI 입력 처리
   - argparse로 --date 인자 받기
   - 날짜 형식 검증

2. Gemini 추천 로직
   - 여행 날짜를 기반으로 도시 1개 추천
   - JSON 형식으로 응답하도록 강제
   - 파싱 실패 시 1회 재시도

3. Kakao Local 검색 로직
   - 추천 도시 + 맛집 키워드로 검색
   - 5개 결과 기준 수집
   - 401/403, 0건, 네트워크 오류 예외 처리

4. 최종 리포트 생성
   - 추천 지역, 날씨, 행사, 맛집, 일정, 오류 요약 포함
   - Markdown 형식으로 저장

5. 결과 저장
   - results/ 폴더 생성
   - YYYY-MM-DD_raw.json 저장
   - YYYY-MM-DD_travel_plan.md 저장

## 3. 예외 처리 정책

- API 키 미설정: 즉시 종료
- LLM JSON 파싱 실패: 재시도 1회 후 기본값 사용
- Kakao API 인증 실패: 에러 기록 후 계속 진행
- 검색 결과 0건: 맛집 없음으로 처리 후 리포트 생성
- 네트워크 오류: 에러 목록에 기록하고 리포트 작성 종료

## 4. 산출물

- travel_planner.py
- README.md
- .env
- .env.example
- requirements.txt
- .gitignore
- results/ 저장 결과물

## 5. 검증 방법

다음 절차로 검증한다.

1. 라이브러리 설치
2. .env 파일에 키 등록
3. 정상 날짜 입력으로 실행
4. 잘못된 날짜 입력으로 오류 처리 확인
5. 결과 파일 생성 여부 확인
6. results/ 폴더의 JSON과 Markdown 파일 확인

## 6. 기대 결과

- CLI로 실행 가능한 프로그램 완성
- 외부 API 연동 구조 확인
- 결과물 저장과 예외 처리 동작 검증
- 미션 요구사항에 맞는 제출물 확보
